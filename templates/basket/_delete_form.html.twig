<form method="post" action="{{ path('basket_delete', {'id': basket.id}) }}" onsubmit="return confirm('Are you sure you want to delete this shirt?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ basket.id) }}">
    <button class="btn btn-secondary d-inline"><i class="bi bi-trash"></i></button>
</form>
