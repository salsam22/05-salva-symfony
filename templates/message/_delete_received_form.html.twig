<form method="post" action="{{ path('message_delete', {'id': receive.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ receive.id) }}">
    <button class="btn btn-outline-secondary ms-2">Delete</button>
</form>
