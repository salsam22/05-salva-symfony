{% extends 'base.html.twig' %}

{% block title %}Shirt{% endblock %}

{% block body %}
    <h1>Shirt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shirt.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ shirt.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ shirt.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ shirt.image }}</td>
            </tr>
            <tr>
                <th>Upload_date</th>
                <td>{{ shirt.uploadDate ? shirt.uploadDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shirt_index') }}">back to list</a>

    <a href="{{ path('shirt_edit', {'id': shirt.id}) }}">edit</a>

    {{ include('shirt/_delete_form.html.twig') }}
{% endblock %}
